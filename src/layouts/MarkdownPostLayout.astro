---
import Layout from "./Layout.astro";

const { frontmatter } = Astro.props;
---

<Layout>
  <div class="mt-10">
    <h1 class="text-3xl font-bold mb-8" transition:name="blogTitle">{frontmatter.title}</h1>
    <div class="prose">
      <slot />
    </div>
  </div>
</Layout>
